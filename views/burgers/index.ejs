<%- include('../partials/header') %>

<table id="list">
    <thead>
      <tr>  
        <th>Burger Name</th>
      </tr>
    </thead>
    <tbody>
      <% burgers.forEach(function(b) { %>
        <tr>
          <td class="burger-user"><img alt="avatar" src="<%= b.userAvatar %>"><%= b.userName %></td>
          <td><%= b.createdAt %></td>
          <td><%= b.name %></td>
          <td><a href="/burgers/<%= b._id %>">Details</a></td>
        <td>   
        <% if (user) { %>
            <form action="/burgers/<%= b._id %>?_method=DELETE" method="POST">
              <button type="submit">Delete Burger</button>
            </form>
           <% } %>
        </td>
        </tr>  
        <% }); %>  
      </tbody>
    </table>


<%- include('../partials/footer') %>